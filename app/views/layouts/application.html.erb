<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Tourbases</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    
    <%= stylesheet_link_tag "https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css"%>
    <%= stylesheet_link_tag "application", media: 'all', "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    
  </head>

  <body>
    <header class="p-3 bg-dark text-white">
      <div class="container">
        <nav class="d-flex flex-wrap align-items-center justify-content-center justify-content-lg-start">
          <ul class="nav col-12 col-lg-auto me-lg-auto mb-2 justify-content-center mb-md-0">
            <li><%= link_to 'Главная', root_path, class: 'nav-link px-2 text-white' %></li>
            <li><%= link_to 'Турбазы', turbasas_path, class: 'nav-link px-2 text-white' %></li>
            <li><%= link_to 'Страны', countries_path, class: 'nav-link px-2 text-white' %></li>
            <li><%= link_to 'Регионы', regions_path, class: 'nav-link px-2 text-white' %></li>
          </ul>
        </nav>
      </div>
    </header>

    <div class="container mt-3">
      <div class="row">
        <%= yield %>
        <%= turbo_frame_tag 'remote-modal' %>
      </div>
    </div>
  </body>
</html>
